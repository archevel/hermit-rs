[package]
name = "mman-vsock-repro"
version = "0.1.0"
edition = "2021"

[target.'cfg(target_os = "hermit")'.dependencies]
hermit = { path = "../../hermit", default-features = false, features = ["acpi", "loader", "vsock", "mman"] }
hermit-abi = { path = "../../hermit-abi" }
